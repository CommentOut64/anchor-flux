--extra-index-url https://download.pytorch.org/whl/cu118

# Core Requirements
python-dateutil
tqdm
click
colorama

# Numerical & Math (Strictly < 2.0.0 for compatibility)
numpy==1.26.4
scipy
sympy

# PyTorch (Explicitly versioned for CUDA 11.8)
torch==2.4.0+cu118
torchaudio==2.4.0+cu118
torchvision==0.19.0+cu118

# Audio Processing
librosa
soundfile
ffmpeg-python
# Replaced 'av' with specific version if needed, keeping latest for now
av

# AI / ML Inference Engines
# Note: Using nvidia-libs below to satisfy ctranslate2 CUDA 12 requirement inside a CUDA 11 Env
faster-whisper==1.2.1
ctranslate2==4.4.0
onnxruntime-gpu==1.18.0

# Source Separation
demucs==4.0.1

# VAD (Voice Activity Detection)
silero-vad

# Server / API (Kept from original list)
fastapi
uvicorn[standard]
python-multipart
sse-starlette
psutil

# Embedded NVIDIA Runtime Libraries (DLLs for Embed Python)
# These allow CTranslate2/Faster-Whisper to run even if system CUDA is missing or v11
nvidia-cublas-cu12
nvidia-cudnn-cu12