### 第一部分：新版 requirements.txt (清理 WhisperX，集成 Faster-Whisper)

**请保存为 `requirements.txt`：**

```text
--extra-index-url https://download.pytorch.org/whl/cu118

# --- 核心环境 ---
python-dateutil
tqdm
click
colorama
filelock
psutil

# --- 数值计算 (严格锁定兼容性) ---
# 必须 < 2.0.0 以兼容现有生态
numpy==1.26.4
scipy
sympy

# --- PyTorch (CUDA 11.8) ---
# 包含 CUDA 11 运行库
torch==2.4.0+cu118
torchaudio==2.4.0+cu118
torchvision==0.19.0+cu118

# --- AI 推理引擎 (混合 CUDA 环境) ---
# Faster-Whisper 1.2.1 需要 CTranslate2 4.x
faster-whisper==1.2.1

# 锁定 CTranslate2 版本以配合下面的 CUDA 12 库
ctranslate2==4.4.0

# --- 关键：嵌入式 CUDA 12 运行库 ---
# 虽然 PyTorch 用的是 cu11，但 CTranslate2 需要这些 cu12 的 DLL。
# pip 安装会将它们放在 site-packages/nvidia 中，实现完美共存。
nvidia-cublas-cu12
nvidia-cudnn-cu12

# --- ONNX (兼容性配置) ---
onnxruntime-gpu==1.18.0

# --- 音频处理 ---
librosa
soundfile
ffmpeg-python
av

# --- 人声分离 ---
demucs==4.0.1

# --- VAD & Web 服务 ---
silero-vad
fastapi
uvicorn[standard]
python-multipart
sse-starlette
```

-----

### 第二部分：格式化后的《Video to SRT GPU 项目迁移开发文档》

以下文档基于您提供的原始内容进行了重新排版，修复了列表、代码块和表格的显示问题，使其成为一份标准的开发指南。

-----

# Video to SRT GPU 项目迁移开发文档

| 属性 | 详情 |
| :--- | :--- |
| **文档版本** | v1.0 |
| **创建日期** | 2025-12-05 |
| **适用项目** | `video_to_srt_gpu` |
| **迁移目标** | 从 Miniconda + WhisperX 迁移到 UV + Faster-Whisper + 嵌入式 Python |

-----

## 1\. 概述

### 1.1 迁移背景和目标

[cite_start]本项目当前使用 Miniconda 环境管理和 WhisperX 作为核心转录引擎。为了实现更好的依赖管理、解决版本冲突问题，并为未来的打包发布做准备，我们需要进行以下迁移 [cite: 3]：

**主要目标：**

1.  **包管理器迁移**：从 Miniconda 迁移到 **UV**（更快、更可靠的 Python 包管理器）。
2.  **转录引擎替换**：从 WhisperX 迁移到 **Faster-Whisper**（更轻量、更易维护）。
3.  **依赖冲突解决**：解决 NumPy 2.x 兼容性问题和其他版本冲突。
4.  **嵌入式部署**：配置嵌入式 Python 环境，为打包发布做准备。
5.  **CUDA 库嵌入**：将 CUDA 和 cuDNN 运行库嵌入项目，减少用户环境依赖。

### 1.2 技术栈变化对比

| 组件 | 迁移前 | 迁移后 | 变化原因 |
| :--- | :--- | :--- | :--- |
| **包管理器** | Miniconda | **UV** | 更快的依赖解析，更好的锁定机制 |
| **Python 环境** | 系统全局/Conda 环境 | **嵌入式 Python + .venv** | 便携性，打包友好 |
| **转录引擎** | WhisperX 3.3.4 | **Faster-Whisper 1.2.1** | 更轻量，无需强制对齐模型 |
| **PyTorch** | 2.5.1 (混合版本) | **2.4.0+cu118 (统一)** | 版本统一，兼容性更好 |
| **NumPy** | 2.2.5 (不兼容) | **1.26.4** | 解决生态兼容性问题 |
| **ONNX Runtime** | 1.21.1 (CPU) | **1.18.0 (GPU)** | 支持 GPU 加速 |
| **CUDA 库** | 依赖系统安装 | **嵌入 DLL 文件** | 减少用户环境要求 |

### 1.3 预期收益

  * **开发体验改善**：
      * ✅ 依赖安装速度提升 3-5 倍（UV vs Conda）。
      * ✅ 版本冲突自动检测和解决。
      * ✅ 精确的依赖锁定 (`uv.lock`)。
  * **部署和分发**：
      * ✅ 项目可直接打包为"解压即用"的整合包。
      * ✅ 用户无需安装 CUDA Toolkit。
  * **代码维护**：
      * ✅ 移除 WhisperX 复杂的对齐逻辑。
      * ✅ 代码更简洁，依赖更少。

-----

## 2\. 环境准备

### 2.1 硬件要求

  * **显卡**：NVIDIA 显卡（支持 CUDA 11.8 或更高）。
  * **显存**：至少 4GB（推荐 8GB 以上）。
  * **内存**：至少 16GB。

**检查显卡驱动版本：**

```powershell
nvidia-smi
```

> [cite_start]**注意**：查看右上角的 `CUDA Version`，确保 \>= 11.8。此处显示的是驱动支持的最高版本，不是实际安装的 Toolkit 版本。我们将使用嵌入式的 CUDA 11.8 和 CUDA 12 运行库 [cite: 3]。

### 2.2 软件要求

1.  **Python 3.10**（⚠️ 不要使用 3.12，有兼容性问题）。
2.  **UV 包管理器**（最新版本）。
3.  **Git**（用于版本控制）。

**安装 UV（Windows）：**

```powershell
# 方法 1：使用 PowerShell（推荐）
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# 方法 2：使用 pip
pip install uv

# 验证安装
uv --version
```

### 2.3 目录结构规划

迁移后的项目目录结构如下：

```text
F:/video_to_srt_gpu/
├── .venv/                      # UV 创建的虚拟环境（包含 Python 和所有依赖）
│   ├── Scripts/
│   │   └── python.exe          # Python 解释器
│   └── Lib/
│       └── site-packages/
│           ├── torch/lib/      # PyTorch 的 CUDA 11 DLL
│           └── nvidia/         # NVIDIA CUDA 12 运行库 (供 CTranslate2 使用)
├── backend/                    # 后端代码
│   └── app/
├── tools/                      # 外部工具
│   ├── ffmpeg.exe
│   └── ffprobe.exe
├── models/                     # 模型文件
├── pyproject.toml              # UV 项目配置
├── uv.lock                     # 依赖锁定文件
├── run_app.bat                 # 启动脚本（新）
└── _archived/                  # 归档的旧文件
    └── whisperx_related/
```

-----

## 3\. 第一步：安装和配置 UV

### 3.1 初始化 UV 项目

在项目根目录执行：

```powershell
cd F:\video_to_srt_gpu

# 初始化 UV 项目
uv init

# 指定 Python 版本
uv python pin 3.10
```

这会创建 `pyproject.toml` 文件。

### 3.2 配置 UV 使用本地虚拟环境

**关键配置**：确保虚拟环境在项目目录内，并使用文件复制而非符号链接（便于打包）。

**推荐方法：创建 `.env` 文件**
在项目根目录创建 `.env` 文件，内容如下：

```ini
UV_LINK_MODE=copy
UV_PROJECT_ENVIRONMENT=.venv
```

### 3.3 创建虚拟环境

```powershell
uv venv .venv
```

**验证**：检查 `.venv` 目录是否已创建。

-----

## 4\. 第二步：构建 UV 环境

### 4.1 依赖安装顺序（重要！）

**原则：先安装底层依赖，再安装上层应用。**

#### 步骤 1：安装 PyTorch（CUDA 11.8 版本）

```powershell
uv pip install torch==2.4.0 torchaudio==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu118
```

*原因：PyTorch 包含 CUDA 运行库 DLL，后续库会依赖它。*

#### 步骤 2：安装 NVIDIA 运行库（嵌入式 CUDA 12）

```powershell
uv pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
```

[cite_start]*原因：提供额外的 CUDA 12 运行库，用于兼容需要 CUDA 12 的 `ctranslate2` 和 `faster-whisper` [cite: 2, 3]。*

#### 步骤 3：安装核心依赖（NumPy）

```powershell
uv pip install "numpy<2.0.0" scipy sympy
```

[cite_start]*关键：必须锁定 `numpy<2.0.0`，否则会导致大量兼容性问题 [cite: 2]。*

#### 步骤 4：安装 AI 推理引擎

```powershell
uv pip install faster-whisper==1.2.1 ctranslate2==4.4.0 onnxruntime-gpu==1.18.0
```

#### 步骤 5：安装其余依赖

```powershell
uv pip install librosa soundfile ffmpeg-python av demucs==4.0.1 silero-vad
uv pip install fastapi uvicorn[standard] python-multipart sse-starlette psutil
uv pip install python-dateutil tqdm click colorama
```

### 4.2 验证环境

创建测试脚本 `test_env.py` 并运行：`uv run python test_env.py`

```python
import sys
import torch
import numpy as np
from faster_whisper import WhisperModel
import onnxruntime as ort

print(f"Python: {sys.version}")
print(f"PyTorch: {torch.__version__}")
print(f"CUDA Available: {torch.cuda.is_available()}")
print(f"NumPy: {np.__version__}")
print(f"ONNX Providers: {ort.get_available_providers()}")
```

**预期结果**：CUDA 为 True，NumPy 版本为 1.26.x，ONNX 包含 `CUDAExecutionProvider`。

-----

## 5\. 第三步：移除 WhisperX 依赖

### 5.1 代码修改清单

| 文件 | 修改类型 | 优先级 |
| :--- | :--- | :--- |
| `backend/app/services/transcription_service.py` | **重构核心逻辑** | 🔴 高 |
| `backend/app/services/model_preload_manager.py` | 移除 WhisperX 模型管理 | 🔴 高 |
| `backend/app/core/logging.py` | 清理日志过滤器 | 🟢 低 |

### 5.2 核心文件修改示例：transcription\_service.py

**原代码（WhisperX）：**

```python
import whisperx
# ... 加载模型，转录，然后加载对齐模型，最后执行 whisperx.align ...
```

**新代码（Faster-Whisper）：**

```python
from faster_whisper import WhisperModel

# 加载模型
model = WhisperModel(model_name, device=device, compute_type=compute_type)

# 转录 (Faster-Whisper 直接返回带时间戳的结果)
segments, info = model.transcribe(
    audio_file,
    language=language,
    beam_size=5,
    vad_filter=True,      # 内置 VAD
    word_timestamps=True  # 词级时间戳
)

# 转换为列表
segments = list(segments)
```

[cite_start]**关键差异**：Faster-Whisper 不需要单独的对齐步骤，内置了 VAD 和时间戳生成 [cite: 3]。

-----

## 6\. 第四步：嵌入式环境配置

### 6.1 嵌入 FFmpeg

1.  下载 FFmpeg (Windows build)。
2.  将 `ffmpeg.exe` 和 `ffprobe.exe` 放入项目的 `tools/` 目录。

### 6.2 创建启动脚本 (run\_app.bat)

此脚本至关重要，它负责设置环境变量，让 Python 找到嵌入的 CUDA DLL。

```batch
@echo off
setlocal EnableDelayedExpansion

echo [INFO] 正在启动 Video to SRT GPU...

:: 1. 获取项目根目录
set "PROJECT_ROOT=%~dp0"
set "PROJECT_ROOT=%PROJECT_ROOT:~0,-1%"

:: 2. 设置 Python 路径
set "VENV_ROOT=%PROJECT_ROOT%\.venv"
set "PYTHON_EXEC=%VENV_ROOT%\Scripts\python.exe"

:: 3. 设置 CUDA DLL 路径（混合环境关键！）
:: PyTorch 的 CUDA 11 库
set "TORCH_LIB=%VENV_ROOT%\Lib\site-packages\torch\lib"
:: Faster-Whisper 需要的 CUDA 12 库
set "NVIDIA_CUDNN=%VENV_ROOT%\Lib\site-packages\nvidia\cudnn\bin"
set "NVIDIA_CUBLAS=%VENV_ROOT%\Lib\site-packages\nvidia\cublas\bin"

:: 4. 设置工具路径
set "TOOLS_DIR=%PROJECT_ROOT%\tools"

:: 5. 更新 PATH
set "PATH=%TORCH_LIB%;%NVIDIA_CUDNN%;%NVIDIA_CUBLAS%;%TOOLS_DIR%;%PATH%"

:: 6. 启动服务
cd "%PROJECT_ROOT%\backend"
"%PYTHON_EXEC%" -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

-----

## 7\. 第五步：归档与清理

为了保持项目整洁：

1.  **创建归档目录**：`mkdir -p _archived/whisperx_related`。
2.  **移动旧代码**：将 `video_to_srt_old.py` 等旧文件移入归档目录。
3.  **锁定依赖**：
    
    ```powershell
    uv pip freeze > requirements-lock.txt
    ```

-----

## 8\. 常见问题 (Troubleshooting)

**Q: 启动时报错 `Could not load library cudnn_ops_infer64_8.dll`？**

  * **原因**：CTranslate2 或 ONNX Runtime 找不到 CUDA 库。
  * **解决**：确保 `run_app.bat` 中的 `PATH` 设置正确，且包含了 `nvidia/cudnn/bin` 和 `torch/lib` 路径。

**Q: 报错 `numpy.dtype size changed, may indicate binary incompatibility`？**

  * **原因**：安装了 NumPy 2.0.0+。
  * **解决**：强制降级 `uv pip install "numpy<2.0.0"`。

-----